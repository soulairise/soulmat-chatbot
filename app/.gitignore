# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 네이버 커머스 API 키
.env

# 네이버 커머스 API 원본 응답 — 절대 커밋 금지.
# 주문/문의 API 응답에는 구매자 이름·주소·연락처가 들어있고
# 이 저장소는 GitHub Pages 때문에 공개다.
# 사람이 확인해 만든 청크(chunks.source.json)만 저장소에 들어간다.
src/data/sources/
